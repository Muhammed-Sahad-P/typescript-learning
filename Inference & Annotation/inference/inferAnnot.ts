//Type Inference (implicit type inference)
   // *TypeScript can automatically infer the type based on the assigned value.
   //! eg; let username = "sachin";
   //!     let age = 25;
   //!     let isMarried = false;


//Type Annotation (explicit type inference)
  //*We can explicitly(manually) annotate the type of a variable   
  //! eg; let username: string = "sachin";
  //!     let age: number = 25;
  //!     let isMarried: boolean = false;